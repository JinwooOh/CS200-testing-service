import React, { Component } from 'react';
import Nav from '../Nav';

export default class QuationListPage extends Component {
  render() {
    return (
      <div>
        <Nav />
      </div>
    );
  }
}
