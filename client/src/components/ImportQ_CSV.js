import React, { Component } from "react";
import Nav from "./Nav";

export default class ImportQ_CSV extends Component {
  render() {
    return (
      <div>
        <h4>Import CSV</h4>
        <input type="file" name="image" />
      </div>
    );
  }
}
